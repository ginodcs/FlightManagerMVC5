using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ARQ.Maqueta.Entities
{
    public class DatabaseInitializerHelper
    {
        public static void Seed(EntitiesDB context)
        {
            /* Enter your database initialization code. For example:
             * for (int i = 0; i < 100; i++)
             * {
             *    User user = new User
             *    {
             *        Name = "My User",
             *        Birthdate = DateTime.Now.AddYears(27)
             *    };
             *   context.UserSet.Add(user);
             * }
             */
        }
    }
}
